# Juego-en-Consola
dotnet new console #Crea un nuevo proyecto a partir del template console.
dotnet new gitignore #Crea el archivo git ignore para que no se publique lo que no es necesario

dotnet run #Ejecuta el proyecto